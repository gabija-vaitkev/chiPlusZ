% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/chiPlusZ.R
\name{crosstabs}
\alias{crosstabs}
\title{Run SPSS-style cross-tabulation with column proportion Z-tests}
\usage{
crosstabs(
  data,
  row_var,
  col_var,
  row_order = NULL,
  col_order = NULL,
  alpha = 0.05,
  export_excel = FALSE,
  excel_path = "crosstabs_output.xlsx"
)
}
\arguments{
\item{data}{A data frame in long format.}

\item{row_var}{Character string naming the \strong{row} variable (categories
displayed as table rows, e.g. \code{"Commute"}).}

\item{col_var}{Character string naming the \strong{column} variable (groups
to compare, e.g. \code{"AgeGroup"}).}

\item{row_order}{Optional character vector giving the desired row order.
Defaults to alphabetical.}

\item{col_order}{Optional character vector giving the desired column order.
Defaults to alphabetical.}

\item{alpha}{Numeric. Significance level for Bonferroni-corrected tests
(default \code{0.05}).}

\item{export_excel}{Logical. If \code{TRUE}, results are written to an Excel
workbook (default \code{FALSE}).}

\item{excel_path}{Character. Path/filename for the Excel output
(default \code{"crosstabs_output.xlsx"}).}
}
\value{
An S3 object of class \code{"crosstabs"} (invisibly), a named list
containing:
\describe{
\item{\code{counts}}{Integer matrix - the contingency table of raw counts.}
\item{\code{row_pct}}{Numeric matrix - row percentages (sum to 100 across cols).}
\item{\code{expected}}{Numeric matrix - expected counts under H0.}
\item{\code{chi2}}{Named numeric vector: \code{chi2}, \code{df}, \code{p}.}
\item{\code{cramers_v}}{Named numeric vector: \code{V}, \code{chi2}, \code{df}, \code{p}.}
\item{\code{letters}}{Character matrix - CLD letters per cell.}
\item{\code{display}}{Character matrix - combined \code{"Count (row\%) <letter>"} strings.}
\item{\code{p_table}}{Data frame - all pairwise raw and Bonferroni p-values.}
\item{\code{low_expected_warning}}{Logical - \code{TRUE} if any expected count < 5.}
}
}
\description{
Builds a two-way contingency table from a long-format data frame, runs an
overall Chi-squared test, computes Cramér's V, performs pairwise
column-proportion Z-tests (Bonferroni-corrected), and produces a compact
letter display (CLD) - mirroring SPSS's \strong{Compare Column Proportions}
output.
}
\examples{
print(result_excel)

}
